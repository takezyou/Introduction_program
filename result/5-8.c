#include <stdio.h>
#include <math.h>
#define nmax 100

double kans(double);

int main(void){
    int i
    double xa, xb, dx, x, f;

    xa = -1;
    xb = 1
    dx = (xb - xa)/(double)nmax;
    x = xa + dx * (double)i;
    f = f(x)
    printf("%e %e \n", x, f);
}