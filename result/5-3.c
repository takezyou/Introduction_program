#include <stdio.h>
#include <math.h>
#dfine max 100

double f (dobule x);

int main () {
  double a, b, xa, xb, dx;
  
  a = 3.0;
  b = 15.0
  
  xa = f(a);
  xb = f(b);
  dx = f((a+b)/2);
  for (i=0; i<=max; i++){ 

  }
}

double f (double x) {
  double f;
  f = x - cox(x);
  
  return f;
}
